# Training / experiment dependencies (ISS-05).
# Includes serve stack plus MLflow, plotting, TensorFlow (quantize experiments), tqdm.

-r requirements-serve.txt

tqdm==4.67.1
matplotlib==3.10.0
seaborn==0.13.2
mlflow==2.20.0
tensorflow==2.18.0
